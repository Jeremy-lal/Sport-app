import { RouterLinkDirectiveStub } from './router-link-stub.directive';

describe('RouterLinkStubDirective', () => {
  it('should create an instance', () => {
    const directive = new RouterLinkDirectiveStub();
    expect(directive).toBeTruthy();
  });
});
